set(FILE_SET_COMMON
    ${CMAKE_CURRENT_SOURCE_DIR}/Fls.c
)

set(FILE_SET_ADAPTER
    ${CMAKE_CURRENT_SOURCE_DIR}/QSF_ADAPTER.c
)

set_target_properties(QSF_COMMON PROPERTIES
    RESOURCE "${FILE_SET_COMMON}"
)

set_target_properties(QSF_ADAPTER PROPERTIES
    RESOURCE "${FILE_SET_ADAPTER}"
)
